#include <iostream>
#include <cstring>

using namespace std;

int n, k;
char cuv, u;

int main(){

    cin >> n >> k;
    for(int i = 1;i <=n;i++){
        cin >> cuv;
        ui = strlen(cuv)-1;
        u = cuv[ui-1];
        if()
    }

    return 0;
}